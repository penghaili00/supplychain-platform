supplychain:
  security:
    jwt:
      issuer: supplychain
      secret: ${SUPPLYCHAIN_JWT_SECRET:SupplyChain-Default-Jwt-Secret-Please-Change-Now-2026}
      access-expire: 30m
      refresh-expire: 7d
      single-login: true
